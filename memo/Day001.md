STEP 1. 철통 보안 시스템 구축 (완료 ✅)
목표: 아무나 들어오지 못하게 막고, 허가된 사용자만 들이기.

구현: Spring Security, JWT, 로그인/회원가입, 권한 관리(Role).

의의: "이제 우리 서버는 해커로부터 안전하고, 누가 누구인지 식별할 수 있습니다."

STEP 2. 게시판 기능 구현 (현재 위치 📍)
목표: 로그인한 사용자가 DB에 글을 쓰고(C), 읽고(R), 수정하고(U), 지우는(D) 기본기 다지기.

핵심 기술:

연관관계 매핑(@ManyToOne): "이 글은 누가 썼지?" (User와 Post 연결)

Auditing: "언제 썼지?" (생성시간/수정시간 자동화)

페이징(Paging): 글 1000개를 한 번에 가져오지 않고 10개씩 끊어오기.

STEP 3. 예약 시스템 (핵심 비즈니스 로직)
목표: 이 프로젝트의 진짜 목적인 '예약(Reservation)' 기능 구현.

핵심 기술:

동시성 제어: "마지막 남은 1자리를 두 명이 동시에 누르면 어떻게 막지?" (Transaction, Lock)

복잡한 조회: "내 예약 내역만 가져오기", "날짜별 예약 가능 여부 확인"

STEP 4. 배포 및 운영 (옵션)
목표: 내 컴퓨터(Localhost)에서만 도는 게 아니라, 실제 클라우드(AWS 등)에 올리기.

# Day 001


### 오늘 한 일
- 너무 많이 했다. 기초 설계부터 
- 지금이 새벽 2시 반이 넘어가는데 잠은 자야하니 일단 간단하게 기록만 남겨보도록 하자
- 뭘했는지만 빠르게 자세한 정리는 내일이나 모레 중으로 해야겠다. 그래도 최대한 빨리

- 1. JWT를 활용한 로그인 처리 구현
    - 필터 체인, 토큰 발행, 토큰 권한 설정  
    - 시큐리티컨피그 설정
    - 그리고 refresh token 구현
    - 기본적인 게시판 크루드
    - 레코드 타입 공부 좀 했고
    
- 2. 프로젝트 확장성? 유지보수성?을 위해 한 작업들
    - 글로벌 에러 처리
    - 에러 코드 enum
    - BusinessException 만들기
    - 시큐리티 Util 만들어서 authentication 관리하기
    - DTO 분리하기
    - Getter / Setter 남용하지 않기 - 팩토리 메소드를 만들어서 활용하기


### 내일? 앞으로 할 일
- 이제 쿠폰? 예약? 시스템 해야겠다. 
- 오늘 하루 동안 열심히 한 것 같고, 근데 이제 사실상 1일 지났다. ㅋㅋ
- 그래도 기반 공사를 다진 것 같으니깐. 일단 오늘은 여기서 마무리 내일부터 이 기반 시스템을 활용해서 해야겠다. 그리고 AI랑 완전 싸웠는데 조금은 사용법을 배운 것 같다. 이것 저것 시도 중
- 그리고 AI도 이상하게 하는 게 조금 있는 듯
- 그리고 조금의 문제가 내가 하는게 시스템은 동작을 하는데 정확히 알고 하는 건지 모르겠지만
- 그래도 이걸 기반으로 다져야겠다
- 이 프로젝트의 궁극의 목적은 머리 박치기 이다


### 제일 난관이였던 거
- Filter가 너무 중요한 듯
- 이거 return 안했다가 화면 하얀색만 나오고 
- 그리고 여기서 권한 설정이 안되고 SecurityConfig에서 403에러 떨어져서 (사실 인건 접근 불가 코드인데) -> 이걸 401로 바꿔야 refresh token 재발급 받고
- 근데 갑자기 든 생각인데 그러면 인증 안되면 이제 401로 에러 떨어지는건가?
    - 인증이 안되면 401로 떨어지는거고 
    - SecurityConfig에서 403으로 떨어지는걸 401로 바꾸는거고
    - 토큰이 유효한 로그인한 사용자가 들어와도 서비스 로직에서 403을 뱉음
        - **그니깐 사용자 권한 확인 로직은 정말 중요한 것 같다!!!!**
        - 일단은 이렇게 이해하고 있어보자!!


### 수고했어 오늘도!!!